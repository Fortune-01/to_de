#include "main.h"
/**
 * main - print all alphabets in lower case
 * Return: Always 0
 */
void print_alphabet(void)

{
	char f;

	for (f = 'a'; f <= 'z'; f++)

	{
		_putchar(f);
	}
	_putchar('\n');
}
